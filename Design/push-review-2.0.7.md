# 2.0.7 (22) 推送前审查 · 2026-09-07

范围：本地未发布的 Token 活跃度、账户用量读取及 Antigravity 菜单栏符号调整。
保持现有版本号，不修改已安装 App，不创建 GitHub Release。

## 已修复

- 可选用量 RPC 静默时，poll 按 4.5 秒宽限期唤醒并保留有效额度；子进程退出也可解析已收数据。
- 区分本次有效空用量与请求缺失/失败；前者清除旧图表，后者保留缓存，兼容旧缓存格式。
- 日统计统一使用公历日期标识；本地当日缺失时不再用前一天 UTC 日期的数据代替。

## 验证

- Swift Package：36 项测试通过，包含缓存往返、空响应及非公历/跨 UTC 日期回归。
- 隔离真实子进程探针：静默 4.51 秒降级、EOF 0.08 秒、不支持接口 0.01 秒、正常用量 0.01 秒，均返回正确额度；不连接真实账户。
- Xcode Release 构建通过；产物包含 x86_64 与 arm64。使用 CODE_SIGNING_ALLOWED=NO，仅为候选构建，不是签名分发包。
- 原生 SwiftUI 离线渲染：英文浅色与中文深色，额度、Token 图表、底部操作无可见遮挡；使用预览数据，不代表真实账户接口或鼠标悬停实测。
- 右键设置修复链路未改动；Provider 数据源与状态保持独立。
- 构建环境存在无关的 iOS Simulator 版本提示及无 AppIntents 依赖的元数据跳过提示，不影响 macOS 构建。

未执行：覆盖安装、真实账户 Token 接口验收、鼠标悬停交互、Developer ID 签名/公证及发布安装包。
